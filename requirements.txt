# CPU PyTorch / NumPy for numeric baselines against the compiler.
# Install with: pip install -r requirements.txt --index-url https://download.pytorch.org/whl/cpu
# Then (not on the pytorch index): pip install "nanobind==2.9.2" pybind11 onnx onnxruntime pytest
# MLIR Python bindings come from the LLVM build via PYTHONPATH (see scripts/env.sh).
torch
numpy
onnx
# optional: onnxruntime  (ORT goldens in scripts/check_numeric.py)
# optional: pytest       (test/e2e)
